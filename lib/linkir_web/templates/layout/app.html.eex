<!DOCTYPE html>
<html lang="en" class="h-full">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <%= csrf_meta_tag() %>
    <%= live_title_tag assigns[:page_title] || "Linkir", suffix: " · Phoenix Framework" %>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <body>
    <header>
      <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
              <%= if @current_user do %>
                <li class="nav-item">
                  <a href="<%= Routes.link_path(@conn, :index) %>" title="Linkir Home Page" class="nav-link">
                    Linkir
                  </a>
                </li>
              <% else %>
                <li class="nav-item">
                  <a href="<%= Routes.page_path(@conn, :index) %>" title="Linkir Home Page" class="nav-link">
                    Linkir
                  </a>
                </li>
              <% end %>
              <%= render "_user_menu.html", assigns %>
            </ul>
          </div>
        </div>
      </nav>
    </header>

    <div class="container">
      <h2><%= dgettext("header", "Introduction") %></h2>
      <p><%= dgettext("header", "Add a new phrase") %></p>
      <p><%= dgettext("header", "Test separate job") %></p>
      <p><%= dgettext("header", "Test separate job 3rd") %></p>
      <p><%= dgettext("header", "Test separate job 4th") %></p>
      <p><%= dgettext("header", "Test separate job 5th") %></p>
      <p><%= dgettext("header", "Test separate job 6th") %></p>
      <button class="btn btn-primary"><%= dgettext("button", "Lunch") %></button>
      <%= @inner_content %>
    </div>

    <footer class="footer">
      <div class="footer_content">
        &copyright; <%= DateTime.utc_now.year %> Linkir - <%= dgettext("footer", "About Us") %>
      </div>
    </footer>
  </body>
</html>
